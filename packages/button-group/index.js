import QwButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
QwButtonGroup.install = function (Vue) {
  Vue.component(QwButtonGroup.name, QwButtonGroup)
}

export default QwButtonGroup
